spring:
  security:
    user:
      name: admin
      password: admin
  application:
    name: admin-server

server:
  port: 6001

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka
    instance:
      hostname: localhost

